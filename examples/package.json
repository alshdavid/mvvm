{
  "name": "examples",
  "private": true,
  "workspaces": [
    "./basic"
  ],
  "scripts": {
    "build": "npx rspack --config ./.config/rspack.config.mjs --mode production",
    "watch": "npx rspack --watch --config ./.config/rspack.config.mjs",
    "serve": "npx http-server ./dist"
  },
  "dependencies": {
    "@rspack/cli": "^1.7.6",
    "@rspack/core": "^1.7.6",
    "html-webpack-plugin": "^5.6.6",
    "http-server": "^14.1.1",
    "preact": "^10.28.3",
    "rxjs": "^7.8.2"
  }
}
